# eslint-config-oogway

Shareable config

## Installation

```
npx install-peerdeps --dev eslint-config-oogway
```

In your `.eslintrc.js` config file, add:

```
module.exports = {
  extends: [
    'oogway',
  ],
};
```
